<?xml version="1.0" encoding="UTF-8"?>
<ValidationRule xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>New_Product_Propose_Quote</fullName>
    <active>true</active>
    <errorConditionFormula>AND(
NOT( $Setup.Bypass_Settings__c.Bypass_Validation_Rules__c ),
RecordType.DeveloperName = &apos;Product&apos;,
 ISPICKVAL(StageName,&apos;Propose / Quote&apos;), 
/*  ISPICKVAL(PRIORVALUE(StageName),&apos;Propose / Quote&apos;), */
OR (

ISPICKVAL(Competition_Positioning__c,&quot;&quot;),
ISBLANK( RFQ_Received_Date__c),
ISBLANK(Application__c),
ISPICKVAL(Category_of_interest__c,&quot;&quot;),
ISBLANK( End_User__c),
ISBLANK(Amount)
)
)</errorConditionFormula>
    <errorMessage>Competition Positioning,
RFQ Received Date and 
Application
are mandatory fields to move to Propose/Quote stage</errorMessage>
</ValidationRule>
