<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Total_Lead_Age__c</fullName>
    <businessStatus>Active</businessStatus>
    <description>IF(
    ISNULL(Conversion_Rejection_Date__c),
    Today() -  DATEVALUE(CreatedDate),
    Conversion_Rejection_Date__c - DATEVALUE(CreatedDate)
)</description>
    <externalId>false</externalId>
    <formula>(
        5 * ( 
            FLOOR(( IF(ISNULL(Conversion_Rejection_Date__c),TODAY(), Conversion_Rejection_Date__c) - DATE( 1900, 1, 7) ) / 7 )
        ) + MIN( 5, 
                MOD( IF(ISNULL(Conversion_Rejection_Date__c),TODAY(), Conversion_Rejection_Date__c) - DATE( 1900, 1, 7), 7 ) )
    ) - (
        5 * ( 
            FLOOR( ( DATEVALUE( CreatedDate ) - DATE( 1900, 1, 7) ) / 7 ) 
        ) + MIN( 5,
                MOD(DATEVALUE( CreatedDate ) - DATE( 1900, 1, 7), 7 ) 
               )
    )</formula>
    <formulaTreatBlanksAs>BlankAsZero</formulaTreatBlanksAs>
    <label>Total Lead Age</label>
    <precision>18</precision>
    <required>false</required>
    <scale>0</scale>
    <trackHistory>false</trackHistory>
    <type>Number</type>
    <unique>false</unique>
</CustomField>
